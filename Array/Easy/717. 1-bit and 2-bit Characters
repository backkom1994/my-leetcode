class Solution {
public:
    bool isOneBitCharacter(vector<int>& bits) {
        int i=0;
        int m=bits.size();
        while(i<m-1){
            if(bits[i]==1)i+=2;
            else i++;
        }
        if(i==m)return false;
        else return true;
    }
};
